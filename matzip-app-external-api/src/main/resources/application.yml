spring:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  profiles:
    include: auth,prod
server:
  port: 8080
cloud:
  aws:
    credentials:
      access-key: access-key
      secret-key: secret-key
    region:
      static: ap-northeast-2
    stack:
      auto: false
    s3:
      bucket: matzip-image
