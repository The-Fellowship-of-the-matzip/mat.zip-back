spring:
  devtools:
    livereload:
      enabled: true
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
    hibernate:
      ddl-auto: none
    open-in-view: false
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:matzip;MODE=MYSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:

admin:
  username: admin
  password: password
